pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git branch: 'main',
                url: 'https://github.com/chandana-9c/Jenkins-projects2'
            }
        }


        stage('Terraform Plan') {
            steps {
                dir('terraform') {	// mention dir('terraform') if terraform files are inside fol called terrraform
                    sh 'terraform init'
                }
            }
        }

        stage('Terraform Plan') {
            steps {
                 dir('terraform') {
                    sh 'terraform plan'
                }
            }
        }

        stage('Terraform Apply') {
            steps {
                dir('terraform') {
                    sh 'terraform apply -auto-approve'
                }
                // sh 'terraform apply -auto-approve'
            }
        }
    }
}

