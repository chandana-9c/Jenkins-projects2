pipeline {
    agent any

    environment {
        APP_NAME = "SampleApp"	// SampleApp.txt
    }

    stages {
        stage('Checkout') {
            steps {
                git branch: "${params.BRANCH}", url: 'https://github.com/chandana-9c/Jenkins-projects2'
            }
        }

        stage('Build') {
            steps {
                script {
                    echo "Building ${APP_NAME} on branch ${params.BRANCH}"
                    sh 'echo "Simulating Build - Build" > build.log'
                }
            }
        }

        stage('Test') {
            steps {
                script {
                    echo "Running tests for branch ${params.BRANCH}"
                    sh 'echo "Simulating Tests - Test" >> build.log'
                }
            }
        }

        stage('Deploy') {
            when {
                branch 'main'
            }
            steps {
                script {
                    echo "Deploying ${APP_NAME} from ${params.BRANCH} to Production"
                    sh 'echo "Simulating Deployment - Deploy" >> build.log'
                }
            }
        }
    }

    post {
        always {
            archiveArtifacts artifacts: 'build.log', allowEmptyArchive: true
            echo "Pipeline completed for ${params.BRANCH}"
        }
    }
}

